/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',

  trailingSlash: true,

  basePath: '/exec_iq',

  assetPrefix: '/exec_iq/',

  reactStrictMode: true,
}

module.exports = nextConfig;
